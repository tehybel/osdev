

- make update rate of graphics faster; currently moving the mouse is laggy
- when run in -nox mode, the system just freezes
- make a switch that can prevent graphics from being enabled

- write a proper subsystem with unit tests

- when a user-mode program panics, we get an assertion failure
